/**
 * mul - multiple two given integers
 * @a: first integer
 * @b: second integer
 * Return: resulting number after multiplying a * b
 */
int mul(int a, int b)
{
	int result = a * b;
	return (result);
}
