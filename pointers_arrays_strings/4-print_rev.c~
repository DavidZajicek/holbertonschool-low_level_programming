#include "main.h"

/**
 * _puts - prints the given string to stdout
 * @str: string to print
 * Return: 0
 */
int _puts(char *str)
{
	while (*str != '\0')
	{
		_putchar(*str);
		str++;
	}
	_putchar('\n');

	return (0);
}
