#include "main.h"

/**
 * main - entry point
 *
 * Return: On success 0.
 */
int main(void)
{
	char output[8] = "_putchar";

	for (i = 0, i == 8, i++)
	{
		_putchar(output[i]);
	}
	_putchar('\n');
	return (0);
}
